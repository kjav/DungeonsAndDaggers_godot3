[gd_scene load_steps=12 format=2]

[ext_resource path="res://assets/bracket.png" type="Texture" id=1]
[ext_resource path="res://assets/light.png" type="Texture" id=2]
[ext_resource path="res://assets/smallcross.webp" type="Texture" id=3]
[ext_resource path="res://assets/bracket_n.png" type="Texture" id=4]
[ext_resource path="res://Menus/scripts/Light.gd" type="Script" id=5]

[sub_resource type="ParticlesMaterial" id=4]
emission_shape = 1
emission_sphere_radius = 25.0
flag_disable_z = true
gravity = Vector3( 0, -25, 0 )
orbit_velocity = 0.0
orbit_velocity_random = 0.0
angle = 720.0
angle_random = 1.0
scale = 0.02
scale_random = 0.1
color = Color( 1, 0.2, 0, 1 )
hue_variation = -0.12
hue_variation_random = 1.0

[sub_resource type="Shader" id=5]
code = "shader_type canvas_item;

uniform float time_offset;
//////////////////////
// Fire Flame shader

// procedural noise from IQ
vec2 hash( vec2 p )
{
	p = vec2( dot(p,vec2(127.1,311.7)),
			 dot(p,vec2(269.5,183.3)) );
	return -1.0 + 2.0*fract(sin(p)*43758.5453123);
}

float noise( in vec2 p )
{
	const float K1 = 0.366025404; // (sqrt(3)-1)/2;
	const float K2 = 0.211324865; // (3-sqrt(3))/6;
	
	vec2 i = floor( p + (p.x+p.y)*K1 );
	
	vec2 a = p - i + (i.x+i.y)*K2;
	vec2 o = (a.x>a.y) ? vec2(1.0,0.0) : vec2(0.0,1.0);
	vec2 b = a - o + K2;
	vec2 c = a - 1.0 + 2.0*K2;
	
	vec3 h = max( 0.5-vec3(dot(a,a), dot(b,b), dot(c,c) ), 0.0 );
	
	vec3 n = h*h*h*h*vec3( dot(a,hash(i+0.0)), dot(b,hash(i+o)), dot(c,hash(i+1.0)));
	
	return dot( n, vec3(70.0) );
}

float fbm(vec2 uv)
{
	float f;
	mat2 m = mat2( vec2(1.6,  1.2), vec2(-1.2,  1.6) );
	f  = 0.5000*noise( uv ); uv = m*uv;
	f += 0.2500*noise( uv ); uv = m*uv;
	f += 0.1250*noise( uv ); uv = m*uv;
	f += 0.0625*noise( uv ); uv = m*uv;
	f = 0.5 + 0.5*f;
	return f;
}

vec4 mainImage( vec2 uv, float time, float strength )
{
	vec2 q = uv - vec2(0.5, 0.2);
	q.x *= 0.5;
	q.y *= 1.2;
	float T3 = max(3.,1.25*strength)*time*0.55;
	float n = fbm(strength*q - vec2(0,T3));
	float c = 1. - 16. * pow( max( 0., length(q*vec2(1.8+q.y*1.5,.75) ) - n * max( -0., q.y+.25 ) ),1.2 );
	float c1 = n * c * (max(1.5-pow(1.3*uv.y,4.), 0.0));
	c1=clamp(c1,0.,1.);

	vec3 col = vec3(1.5*c1, 1.5*c1*c1*c1, c1*c1*c1*c1*c1*c1);
	
	float a = c * (1.-pow(uv.y,3.));
	return vec4( mix(vec3(0.),col,a), col.x);
}

void fragment() {
  COLOR = mainImage(vec2(1, 1) - UV, TIME + time_offset, 1.5);
}"

[sub_resource type="ShaderMaterial" id=6]
shader = SubResource( 5 )
shader_param/time_offset = null

[sub_resource type="ImageTexture" id=7]
size = Vector2( 256, 256 )

[sub_resource type="GDScript" id=8]
script/source = "tool
extends Light2D

var noise

func _ready():
	noise = OpenSimplexNoise.new()
	noise.seed = randi()
	noise.octaves = 4
	noise.period = 20.0
	noise.persistence = 0.8

var time = 0.25
func _process(delta):
	time += delta * 20 * (1 + noise.get_noise_2d(time, 1000.0))
	energy = (noise.get_noise_2d(time, 1.0) + 1) * 2.4
"

[sub_resource type="GDScript" id=9]
script/source = "tool
extends Light2D

var noise

func _ready():
	noise = OpenSimplexNoise.new()
	noise.seed = randi()
	noise.octaves = 4
	noise.period = 20.0
	noise.persistence = 0.8

var time = 0.5
func _process(delta):
	time += delta * 20 * (1 + noise.get_noise_2d(time, 1000.0))
	energy = (noise.get_noise_2d(time, 1.0) + 1) * 1.8
"

[node name="Torch" type="Node2D"]
position = Vector2( 916.705, 1125.75 )

[node name="Handle" type="Sprite" parent="."]
scale = Vector2( 0.2, 0.2 )
texture = ExtResource( 1 )
normal_map = ExtResource( 4 )

[node name="Particles2D" type="Particles2D" parent="."]
position = Vector2( 0, -60 )
scale = Vector2( 2, 2 )
emitting = false
amount = 10
process_material = SubResource( 4 )
texture = ExtResource( 3 )

[node name="Flame" type="Sprite" parent="."]
light_mask = 0
material = SubResource( 6 )
position = Vector2( 0, -127.5 )
scale = Vector2( 0.5, 1 )
texture = SubResource( 7 )

[node name="Light" type="Light2D" parent="."]
position = Vector2( -2, -39.994 )
texture = ExtResource( 2 )
texture_scale = 0.25
color = Color( 1, 0.352941, 0, 1 )
energy = 3.68591
range_height = 40.0
script = ExtResource( 5 )

[node name="Light2" type="Light2D" parent="."]
position = Vector2( -2, -99.994 )
texture = ExtResource( 2 )
texture_scale = 0.25
color = Color( 1, 0.352941, 0, 1 )
energy = 2.18839
range_height = 40.0
script = SubResource( 8 )

[node name="Light3" type="Light2D" parent="."]
position = Vector2( -2, -159.994 )
texture = ExtResource( 2 )
texture_scale = 0.25
color = Color( 1, 0.352941, 0, 1 )
energy = 1.55064
range_height = 40.0
script = SubResource( 9 )
